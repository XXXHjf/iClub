<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 访问网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 读取外部储存卡 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 读、写文件权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 相机权限（相机硬件为可选项） -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestApplication01">
        <activity
            android:name=".createActActivity"
            android:exported="false" />
        <activity
            android:name=".clubManageActivity"
            android:exported="false" />
        <activity
            android:name=".actMineActivity"
            android:exported="false" />
        <activity
            android:name=".actDetailActivity"
            android:exported="false" />
        <activity
            android:name=".actCenterActivity"
            android:exported="false" />
        <activity
            android:name=".homeActivity"
            android:exported="false" />
        <activity
            android:name=".clubMineActivity"
            android:exported="false" />
        <activity
            android:name=".clubDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".personalDataActivity"
            android:exported="false" />
        <activity
            android:name=".clubListActivity"
            android:exported="false" />
        <activity
            android:name=".loginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>